apiVersion: v1
kind: Service
metadata:
  name: {{ include "chtison.fullname" . }}
  labels:
    app: {{ include "chtison.name" . }}
    chart: {{ include "chtison.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "chtison.name" . }}
    release: {{ .Release.Name }}
